// src/app/pipeline/page.tsx
// Recruitment pipeline with Kanban (desktop) and list (mobile) views
// Phase 2 placeholder; will show drag-and-drop columns per recruitment status
// RELEVANT FILES: src/components/pipeline/KanbanBoard.tsx, src/components/pipeline/StatusList.tsx, src/hooks/useAgeGroup.ts

export default function PipelinePage() {
  return (
    <div className="p-4 lg:p-6">
      <h1 className="mb-4 text-xl font-bold lg:text-2xl">Pipeline</h1>
      <p className="text-muted-foreground">
        Pipeline de recrutamento — disponível na Phase 2.
      </p>
    </div>
  );
}
